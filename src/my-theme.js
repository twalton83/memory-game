import { DefaultTheme } from 'styled-components';

const myTheme: DefaultTheme = {
  borderRadius: '5px',
  backgroundColor: '#403e3e',
  colors: {
    main: 'white',
    secondary: 'magenta',
  },
};

export { myTheme };