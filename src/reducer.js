export function reducer(state, action) {
  switch (action.type) {
    case 'INIT':
      return
    case 'SHUFFLE':
      return "";
    default:
      throw new Error();
  }

}

export const init = []